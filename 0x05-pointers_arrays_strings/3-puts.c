#include "main.h"
#include <stdio.h>
/*
 *_puts - print a string to stdout
 * @str: char array string type
 * Description: Can only use _putchar
 */
void _puts(char *str)
{
	puts(str);

	return (0);
}
