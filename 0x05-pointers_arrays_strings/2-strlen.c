#include "main.h"
#include <string.h>
#include <stdio.h>
/*
 *_strlen - return the length of the string
 * @*s: char type pointer
 * Return: length of string
 */
int _strlen(char *s)
{
	_putchar(strlen(*s));
	return (*s);
}
