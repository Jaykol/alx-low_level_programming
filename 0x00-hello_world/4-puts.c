#include <stdio.h>
     /**
      *program to print a text
      * returns (0)
      */
int main(void)
	/**
	 * main -entry point
	 */
	{
		puts("\"Programming is like building a multilingual puzzle");
		return (0);
	}
