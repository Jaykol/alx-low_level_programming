#include <stdio.h>
     /**
      * main function is where code block sites
      *
      * returns (0)
      */
int main(void)
	{
		puts("\"Programming is like building a multilingual puzzle");
		return (0);
	}
