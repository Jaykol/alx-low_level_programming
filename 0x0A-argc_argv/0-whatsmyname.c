#include "main.h"
#include <stdio.h>

/**
 * main - main function to print program name
 * Return: returns 0
 */

int main(int argc, char *argv[])
{
	printf("The program's name is %s \n", argv[0]);
	return (0);
}
